# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0.0-SNAPSHOT
maven_group = dev.encode42
archives_base_name = renewables

# Dependencies are managed at gradle/libs.versions.toml
